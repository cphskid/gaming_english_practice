# 複製成 .env 再填。沒填就用瀏覽器的 localStorage，一樣玩得動，只是換裝置看不到紀錄。
#
# 這兩個值在 Supabase 後台 Project Settings → API。
# VITE_SUPABASE_KEY 要用 sb_publishable_...（以前叫 anon）那把，它本來就是公開的。
# **絕對不要**把 sb_secret_...（以前叫 service_role）填在這裡，那把會繞過所有權限檢查。
VITE_SUPABASE_URL=
VITE_SUPABASE_KEY=
